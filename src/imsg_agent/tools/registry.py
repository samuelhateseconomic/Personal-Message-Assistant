"""Tool Registry — Central dispatch for all AI tool calls.

Registers tool handlers and dispatches incoming tool calls
from the agent orchestrator. Also provides tool schemas in
the JSON format expected by Ollama's tool-calling API.
"""
from __future__ import annotations

# TODO: Implement ToolRegistry class
# TODO: Implement register(name, handler) to add tool handlers
# TODO: Implement get_schemas() -> list[dict] for Ollama
# TODO: Implement execute(name, args) -> dict (dispatch to handler)
