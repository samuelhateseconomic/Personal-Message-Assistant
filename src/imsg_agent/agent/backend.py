"""Ollama Backend — Wrapper around the Ollama Python SDK.

Handles communication with the locally-running Ollama instance:
- Sends chat requests with tool schemas
- Checks if Ollama is running and the model is available
- Auto-pulls the model if not present
"""
from __future__ import annotations

# TODO: Implement OllamaBackend class
# TODO: Implement chat(messages, tools) -> ChatResponse
# TODO: Implement is_available() -> bool (health check)
# TODO: Implement ensure_model_pulled() (pull if missing)
